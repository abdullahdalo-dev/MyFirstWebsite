<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSS Animations Combined Demo</title>

    <style>
        body {
            font-family: Arial, Helvetica, sans-serif;
            background: #f4f4f4;
            text-align: center;
            padding: 40px;
        }

        .wrap {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
            gap: 30px;
            max-width: 600px;
            margin: auto;
        }

        .box {
            background: #8e44ad;
            color: #fff;
            padding: 30px 10px;
            border-radius: 10px;  /* border-radius: 50%; margin: 1rem auto;*/
        }

        /* 1 Floating Box */
        .float {
            animation: float 2s ease-in-out infinite alternate;
        }

        @keyframes float {
            from { transform: translateY(0);}
            to { transform: translateY(-30px);}
        }

        /*  2 Blinking Text */
        .blink {
            animation: blink 1.5s infinite alternate;
        }

        @keyframes blink {
            from {opacity: 0.3;}
            to {opacity: 1;}
        }

        /* 3 Rotating Square */
        .rotate {
            animation: rotate 2s linear infinite;
        }

        @keyframes rotate {
            from {transform: rotate(0deg);}
            to {transform: rotate(360deg);}
        }

        /* Growing Button */
        .grow {
            animation: grow 1.5s ease-in-out infinite; 
        }

        @keyframes grow {
            0% {transform: scale(1);}
            50% {transform: scale(1.2);}
            100% {transform: scale(1);}
        }


    </style>
</head>
<body>
    
    <h2>Mini CSS Animations Demo</h2>

    <div class="wrap">
        <div class="box float">Float</div>
        <div class="box blink">Blink</div>
        <div class="box rotate">Rotate</div>
        <div class="box grow">Grow</div>
    </div>

</body>
</html>
