<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSS + JS Animations</title>

    <style>
        body {
            text-align: center;
            background: #111;
            color: white;
            font-family: 'Courier New', Courier, monospace;
        }

        .container {
            margin-top: 50px;
            display: flex;
            justify-content: center;
            gap: 40px;
        }

        .box {
            width: 80px;
            height: 80px;
            background: #00ff99;
            border-radius: 12px;
        }

        .bounce {animation: bounce 1s infinite alternate;}
        .fade {animation: fade 1.5s infinite alternate;}
        .spin {animation: spin 1.2s linear infinite;}
        .pulse {animation: pulse 1s infinite alternate;}

        /*  ايقاف الانيميشن */
        .stop {
            animation: none !important;
        }


        @keyframes bounce {
            from {transform: translateY(0);}
            to {transform: translateY(-80px);}
        }

        

        @keyframes fade {
            from {opacity: 1;}
            to {opacity: 0;}
        }

        

        @keyframes spin {
            from {transform: rotate(0);}
            to {transform: rotate(360deg);}
        }


        @keyframes pulse {
            from {transform: scale(1);}
            to {transform: scale(1.4);}
        }
    </style>
</head>
<body>
    <h1>CSS Animations with JavaScript</h1>

    <div class="container">
        <div class="box bounce"></div>
        <div class="box fade"></div>
        <div class="box spin"></div>
        <div class="box pulse"></div>
    </div>

    <button onclick="toggleAnimation()">يقاف / تشغيل</button>

    <script>
        let running = true;
        function toggleAnimation() {
            const boxes = document.querySelectorAll('.box');
            boxes.forEach(box => {
                box.classList.toggle('stop');
            });

            running = !running;
        }
    </script>
</body>
</html>
