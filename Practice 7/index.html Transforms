Transforms – 2D & 3D Magic
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>CSS 2D & 3D Transforms Demo</title>
  <style>
    * { box-sizing: border-box; margin:0; padding:0; }
    body { font-family: sans-serif; padding: 2rem; background: #f4f4f4; }

    h1 { text-align: center; margin-bottom: 1.5rem; }

    /* Container for 2D demos */
    .grid {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
      justify-content: center;
    }
    .box {
      width: 100px; height: 100px;
      background: #3498db;
      color: #fff;
      display: flex; align-items: center; justify-content: center;
      transition: transform 0.5s;
    }
    /* 2D transform demos on hover */
    .translate:hover { transform: translate(20px, 20px); }
    .rotate:hover    { transform: rotate(45deg); }
    .scale:hover     { transform: scale(1.5, 0.75); }
    .skew:hover      { transform: skew(20deg, 10deg); }
    .combined:hover  { transform: rotate(-35deg) translateX(30px) scale(1.2); }

    /* 3D scene for card flip */
    .scene {
      width: 200px; height: 120px;
      margin: 3rem auto;
      perspective: 600px; /* depth */
    }
    .card {
      width: 100%; height: 100%;
      position: relative;
      transform-style: preserve-3d;
      transition: transform 0.8s;
    }
    .card:hover {
      transform: rotateY(180deg);
    }
    .face {
      position: absolute;
      width: 100%; height: 100%;
      backface-visibility: hidden;
      display: flex; align-items: center; justify-content: center;
      font-size: 1.2rem; color: #fff;
      border-radius: 8px;
    }
    .front { background: #e74c3c; }
    .back {
      background: #2ecc71;
      transform: rotateY(180deg);
    }
  </style>
</head>
<body>

  <h1>CSS 2D Transform Examples</h1>
  <br>
  <div class="grid">
    <div class="box translate">Translate</div>
    <div class="box rotate">Rotate</div>
    <div class="box scale">Scale</div>
    <div class="box skew">Skew</div>
    <div class="box combined">Combo</div>
  </div>
<br>
<br>

  <h1>CSS 3D Transform – Card Flip</h1>

  <div class="scene">
    <div class="card">
      <div class="face front">Front</div>
      <div class="face back">Back</div>
    </div>
  </div>

</body>
</html>
