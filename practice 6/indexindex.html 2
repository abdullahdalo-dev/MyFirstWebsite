<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Professional Opacity & Transparency</title>

    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: Arial, Helvetica, sans-serif;
        }

        body {
            min-height: 100vh;
            background: linear-gradient(120deg, #1abc9c, #3498db);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
        }

        .container {
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
            justify-content: center;
        }

        .card {
            width: 220px;
            height: 140px;
            border-radius: 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            font-weight: bold;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.25);
            transition: all 0.4s ease;
        }

        /* 1) opacity */
        .card-opacity {
            background: #e74c3c;
            opacity: 0.4;
        }

        /* 2) raba */
        .card-rgba {
            background: rgba(0, 0, 0, 0.4);
            border: 1px solid rgba(255, 255, 255, 0.4);
            backdrop-filter: blur(6px);
        }

        /* 3) Glassmorphism ?/  يعني احترلفي */
        .card-glass {
            background: rgba(255, 255, 255, 0.15);
            border: 1px solid rgba(255, 255, 255, 0.3);
            backdrop-filter: blur(10px);
        }

        /* 4) Hover Transparency */
        .card-hover {
            background: #2c3e50;
        }

        .card-hover:hover {
            opacity: 0.5;
            transform: translateY(-8px);
        }

    </style>
</head>
<body>
    
    <div class="container">
        <div class="card card-opacity">
            Opacity: 0.4 <br>     (  كل العنصر شفاف ) </div>

            <div class="card card-rgba">  rgba background <br>   (  النص واضح )</div>

            <div class="card card-glass">  Glass Effect <br>  (  شفافية حديثة ) </div>

            <div class="card card-hover">  Hover Effect <br>  ( مرر الماوس ) </div>

    </div>
</body>
</html>
