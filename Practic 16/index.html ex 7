<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSS Masking Full Example</title>

    <style>
        body {
            font-family: Arial, Helvetica, sans-serif;
            background: #111;
            color: white;
            text-align: center;
            padding: 40px;
        }

        /* 1- Mask Image + 2- Webkit Prefix */
        .image-mask {
            width: 300px;
            height: 200px;
            margin: 40px auto;
            background: url("https://picsum.photos/500") center/cover;

            -webkit-mask-image: linear-gradient(ot right, black, transparent);
            mask-image: linear-gradient(to right, black, transparent);
       

        /* 3 Mask Positioning & Sizing */
        -webkit-mask-size: 100% 100%;
        mask-size: 100% 100%;
        -webkit-mask-position: center;
        mask-position: center;
    }

    /* 4- Gradients as Masks (Circle) */
    .circle-mask {
        width: 250px;
        height: 250px;
        margin: 40px auto;
        background: url("https://picsum.photos/600")center/cover;
        
        -webkit-mask-image: radial-gradient(circle, black 60%, transparent 70%);
        mask-image: radial-gradient(circle, black 60%, transparent 70%);
    }

    /* 5- Masking Text With an Image */
    .text-mask {
        font-size: 80px;
        font-weight: bold;
        background: url("https://picsum.photos/800") center/cover;
        color: transparent;

        -webkit-background-clip: text;
        background-clip: text;

        /* Mask text using image */
        -webkit-mask-image: linear-gradient(black, black);
        mask-image: linear-gradient(black, black);
    }
    </style>
</head>
<body>
    <h2>1-3     <i>Image Mask + Position & Size</i></h2>
    <div class="image-mask"></div>

    <h2>4 Gradient Mask (Circle)</h2>
    <div class="circle-mask"></div>

    <h2>5 Text Masked With Image </h2>
    <div class="text-mask">Abdullah dalo</div>
    
</body>
</html>
