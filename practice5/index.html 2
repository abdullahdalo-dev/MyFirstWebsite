<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple Shadow Demo</title>

    <style>
        body {
            font-family: Arial, Helvetica, sans-serif;
            background: #eee;
            text-align: center;
            padding: 5px;
        }

        /* Text shadow */
        h1 {
            text-shadow: 2px 2px 4px gray;
        }

        /* Box Shadow */
        .box {
            width: 150px;
            height: 100px;
            margin: 20px auto;
            line-height: 100px;
            box-shadow: 5px 5px 10px rgba(0, 0, 0, 0.3);
        }

        /* Inset Shadow */
        .inset {
            box-shadow: inset 0 0 10px rgba(0, 0, 0, 0.4);
        }

        /* Drop Shadow Image */
        img {
            width: 150px;
            filter: drop-shadow(5px 5px 6px black);
        }
    </style>
</head>
<body>
    <h1>Shadow Effects</h1>
    <div class="box">Box Shadow</div>
    <div class="box inset">Inset Shadow</div>
    <img src="images/ca 1.jpg" alt="imags">
    
</body>
</html>
